import React from "react"

import Aside from "./mdx/aside";

const Prerequisites = props => (
<Aside>

Integrating with Covalent can begin as soon as you create an account, and requires two steps:
<ol>
<li>Obtain your API keys so Covalent can authenticate your integration’s API requests</li>
<li>Make a test API request to confirm everything is up and running</li>
</ol>
</Aside>
)

export default Prerequisites
